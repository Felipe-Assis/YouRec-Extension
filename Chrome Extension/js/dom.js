// Dimensions of the Libras window, expressed as floats:
var windowWidth;
var windowHeight;
var contextWindow;

$(document).ready(function(){

	// Calculates Libras window aspect ratio:
	console.log("Youtube QoS-Aware Recommendations")


});
